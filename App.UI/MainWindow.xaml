<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="JpegViewer.App.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JpegViewer.App.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:control="using:JpegViewer.App.UI.Controls"
    mc:Ignorable="d"
    Title="JpegViewer">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="root">
        <Grid.RowDefinitions>            
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <control:CtrlFolderPicker x:Name="folderPicker"/>
        <control:CtrlPhoto x:Name="photoViewer" Grid.Column="1"/>
        <control:CtrlTimeline x:Name="timeline" Grid.ColumnSpan="2" Grid.Row="1"/>
    </Grid>
</Window>
